class main{
  int least = Integer.MAX_VALUE;
  profit = 0;
  profitToday = 0;
  for(int i = 0 ; array.lenght; i++)
   if(array[i] <least) 
     least = array[i];
 priceToday = array[i] - least;
  if(profit<priceTOday)
    profit = priceTOday;
}
return profit;

time and space -- o(n);
