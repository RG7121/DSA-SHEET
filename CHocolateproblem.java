class Main{
 if( students ==0 | packets == 0)
   return 0;
  if( students> packets) 
    return -1;
  int min = Integer.MAX_VALUE;
  for(int i = 0; i+m-1 <n ;I++)
    int diff = arr[i+m-1] -arra[i]
    if(diff <min)
      min = diff;
  
  return diff;
}
