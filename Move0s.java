Class main{
 if(array.length == 0|| array.length ==1)
   return;
 int left = 0;
  int right = 0;
  for(int i = 0 ; i<array.length;i++)
    if(array[right[ == 0)
            right++;
    else{
      int temp = array[left]
      array[left] = array[right]
       array[right] = temp;
      right++;
      left++;
  
}
