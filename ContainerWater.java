Class Main{
 int max = 0;
  int i = 0;
  int j = array.length-1;
  while(i<j)
    max = Math.max(max, Math.min(arr[i],arr[j]* (j-i));
    if(i<j) i++;
    else j--;
                   
                  return max
}
