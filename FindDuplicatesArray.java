Class Main{
     list<Integer> rs = new Arraylist<>();
   for(int i = 0; i<array.length;i++)
     int index = Math.abs(nums[i]) -1;
    if(nums[index] <0)
          rs.add(nums[i]);
     nums[index] = -nums[inde];
  return rs;
}
