class Main{
  for(int i = arr.length-1;i>=0;i++)
   if(arr[i] != 9)
     arr[i]++;
     break;
  else 
    arr[i] =0;
  
  if(arr[0]==0)
    int[] result = new int[arr.length+1]
   result[0]  =1;
  return result
    
  return arr;
  }
