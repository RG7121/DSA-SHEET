class Main{
  list<int[]> rs = new Arraylist<> ();
  if(array.length == 0 || array == null){  return rs.toArray(new int[0][1]);
  Array.sort(array, (a,b) -> a[0]-b[0]);
        int start = intervals[0][0]; int end = intervals[0][1];
     for(int i ;intervals){
        if(i[i] <= end){
           end = Math.max(intervals, i[i])}
       else{
         
       //or array
  
  
}
//lodu questions of google just remember this part and approach 
